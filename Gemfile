source 'https://rubygems.org'

gem 'jekyll', '~> 4.4'
# Use minima 2.x which is available on rubygems for Jekyll 4.x
gem 'minima', '~> 2.5'

group :jekyll_plugins do
    # minimal plugin set for simple site
end
